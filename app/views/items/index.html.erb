<div class="main posts-index">
  <div class="container">
  Click Item to see details
    <% @items.each do |item| %>
      <div class="posts-index-item">
        <%= link_to("#{item.title} #{item.price} $", item) %>
      </div>
    <% end %>
  <%= link_to 'Post New Item', new_item_path %>
  </div>
</div>